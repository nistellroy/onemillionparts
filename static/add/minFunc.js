
const timeShift = require('./arrFunc');

module.exports = function (array) {
      array.filter((str, index) => {
         return index;
      });

      // for (let i = 0; i < array2.length; i++) {
      //     timeShift(array[i], array2[i]);
      // }

}
